export enum COMMANDS {
  config = 'config',
  hook = 'hook',
  commitlint = 'commitlint',
  check = 'check',
  update = 'update',
  pr = 'pr',
  changelog = 'changelog',
  review = 'review',
  standards = 'standards',
  local = 'local'
}
